// This is a barrel file for exporting Firebase-related hooks and utilities.
// This makes it easier to import them from other parts of the application.

export { useAuth, useAuthService, useDbService } from "@/firebase/provider";
